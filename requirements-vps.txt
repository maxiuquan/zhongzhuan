# =============================================================================
# 由 pyproject.toml 派生，请勿手改。
#
# 对应 pyproject.toml 的 extras: tidb + admin。
# 推荐用法（等价且不会漂移）：
#     pip install ".[tidb,admin]"
#
# 校验一致性：
#     python scripts/check_requirements_sync.py
# =============================================================================
-r requirements.txt

# extras: tidb
aiomysql>=0.2

# extras: admin
PyJWT>=2.8
bcrypt>=4.1
