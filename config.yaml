server:
  proxy:
    host: 127.0.0.1
    port: 8088
  admin:
    host: 127.0.0.1
    port: 8089
limits:
  global_concurrent: 64
  default_rpm_per_key: 60
storage:
  db_path: data.db
  log_dir: logs

# ---------------------------------------------------------------------------
# Hosted tool 能力面（APIAADBPW-REQ-MA-001 / FR-1 / FR-2 / FR-6）
# ---------------------------------------------------------------------------
# tool_search_enabled: 开启后中继自行合成 ``tool_search_output``，把
# ``multi_agent_v1`` namespace 暴露给客户端（Codex 26.x V1 多代理协议）。
# 默认关闭（安全默认：不经显式配置，谁也不该给每会话默认开出多代理编排）。
hosted_tools:
  tool_search_enabled: false

# ---------------------------------------------------------------------------
# V1 多代理编排（APIAADBPW-REQ-MA-001 / FR-3 / FR-4 / NFR）
# ---------------------------------------------------------------------------
# enabled: 总闸。仅当为真且 hosted_tools.tool_search_enabled 为真时，中继才
#   拦截 ``multi_agent_v1`` namespace 调用并就地执行子代理生命周期。
# max_threads: 同一会话内并发子代理上限（NFR-6）。
# job_max_runtime_seconds: 单子代理任务硬上限（NFR-1，铁律 5 客户端 1800s）。
# minimal_client_version: 向 Codex 声明的客户端最低版本（FR-5 / NFR）。
# tool_search_mode: tool_search 结果回传形态（FR-2.1 / 附录 C.10.3，先 A 后 B）：
#   client（默认）= 仅回 tool_search_call(execution="client")，Codex 本地 BM25 命中
#   models.json 的 multi_agent_v1 namespace；server = 外加 function_call_output
#   把中继已知的 namespace 喂回（若 client 方案下 Codex 未暴露 spawn_agent 再切）。
# spawn_execution: spawn_agent 执行归属（FR-9 / 增量修订 v2.0）：
#   client（默认，方案 A）= 中继不代执行，原样透传 function_call 给 Codex 客户端，
#     由本地 SpawnAgentHandler 执行（子代理推理走普通 /v1/responses）；消除
#     23:08 冒烟日志的 unexpected tool output（内联 fco 违反客户端契约）。
#   server（方案 B 兜底）= 保留中继代执行（FR-3），fco 回传子代理产物（FR-10）。
multi_agent:
  enabled: false
  max_threads: 4
  job_max_runtime_seconds: 1800
  minimal_client_version: "0.144.0"
  tool_search_mode: "client"
  spawn_execution: "client"

# ---------------------------------------------------------------------------
# Key 失败退避 + agnes 异步补判（2026-08-15 v1）
# ---------------------------------------------------------------------------
# permanent（401/402/欠费/model_not_found）→ 标记失效，等待管理端「确认恢复」；
# banned（403 CF 封禁）→ 长冷却 600s，自动恢复（也可手动提前恢复）；
# rate_limit / transient → 逐级退避 5s→10s→60s→600s。
# agnes_classify_enabled: 规则无法分类的失败是否异步交给 agnes 补判（默认开）。
# agnes_classify_model: 补判模型（走中继 slug，需免费稳定）。
# agnes_classify_min_interval: 同一 key 同类错误的最小补判间隔（秒）。
key_backoff:
  agnes_classify_enabled: true
  agnes_classify_model: "juhe/agnes-2.5-flash"
  agnes_classify_min_interval: 300
