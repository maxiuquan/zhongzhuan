# =============================================================================
# 由 pyproject.toml 派生，请勿手改。
#
# 依赖的唯一事实源是 pyproject.toml 的 [project].dependencies。
# 本文件仅为「不想用 PEP 517 安装流程」的部署场景保留的便捷入口，
# 内容必须与 pyproject.toml 逐行一致。
#
# 推荐用法（等价且不会漂移）：
#     pip install .
#
# 校验一致性：
#     python scripts/check_requirements_sync.py
# =============================================================================
aiohttp>=3.9,<4
httpx>=0.27,<1
pyyaml>=6.0
loguru>=0.7
aiosqlite>=0.19
cryptography>=42
python-dotenv>=1.0
pydantic>=2.6,<3
